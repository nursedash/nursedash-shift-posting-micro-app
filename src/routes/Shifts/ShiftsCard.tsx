import React, { FC } from 'react';
import { Card, CardContent, Typography } from '@mui/material';

const ShiftsCard: FC = () => (
  <Card sx={{width: '500px'}}>
    <CardContent>
      <Typography>Assisted Living / STNA</Typography>
    </CardContent>
  </Card>
)

export default ShiftsCard;